# VITA-QinYu Requirements (Project Aligned)
# Based on requirements_ds_gpu.txt with recommended versions
# Python: 3.12, CUDA: 12.4

# ============================================================================
# IMPORTANT: Install PyTorch first
# ============================================================================
# pip install torch==2.5.1+cu124 torchaudio==2.5.1+cu124 torchvision==0.20.1+cu124 --index-url https://download.pytorch.org/whl/cu124

# ============================================================================
# Core Requirements (from requirements_ds_gpu.txt)
# ============================================================================
expecttest==0.2.1
peft==0.13.2
XlsxWriter==3.2.0
termcolor==2.5.0
tabulate==0.9.0
tiktoken==0.8.0
matplotlib==3.9.2
einops==0.8.1
pybind11==2.13.6
tensorboardX==2.6.2.2
pyarrow==18.1.0
transformers==4.51.3
deepspeed==0.15.4
accelerate>=1.1.1
timm==1.0.11
Flask==3.1.0
flask-restful==0.3.10
flask-socketio==5.6.1
pyaudio==0.2.14
decord==0.6.0
natsort==8.4.0
setuptools==80.10.1

# ============================================================================
# CosyVoice2 Dependencies
# ============================================================================
pyworld==0.3.5  # Note: v0.3.4 in requirements, but 0.3.5 installed
evaluate==0.4.3
HyperPyYAML==1.2.2
diffusers==0.33.1
conformer==0.3.2
hydra-core==1.3.2
lightning==2.5.1.post0
gdown==5.2.0
wget==3.2
funasr==1.2.6
zhconv==1.4.3
jiwer==3.1.0
zhon==2.1.1
WeTextProcessing==1.0.4.1
inflect==7.5.0
openai-whisper==20240930
onnxruntime==1.22.0
modelscope==1.26.0
word2number==1.1

filetype==1.2.0
fire==0.7.1

# ============================================================================
# Spark-TTS
# ============================================================================
einx==0.3.0

# ============================================================================
# Additional
# ============================================================================
loguru==0.7.3
Cython==3.0.11
wheel==0.45.1

# ============================================================================
# Core packages (not in original requirements but needed)
# ============================================================================
numpy==1.26.4
pillow==10.4.0
opencv-python==4.10.0.84
omegaconf==2.3.0

# ============================================================================
# Installation Instructions
# ============================================================================
#
# 1. Install PyTorch with CUDA 12.4:
#    pip install torch==2.5.1+cu124 torchaudio==2.5.1+cu124 torchvision==0.20.1+cu124 \
#        --index-url https://download.pytorch.org/whl/cu124
#
# 2. Install requirements:
#    pip install -r requirements_aligned.txt
#
# 3. (Optional) Install Apex for mixed precision training:
#    git clone https://github.com/NVIDIA/apex
#    cd apex
#    pip install -v --disable-pip-version-check --no-cache-dir --no-build-isolation \
#        --config-settings "--build-option=--cpp_ext" --config-settings "--build-option=--cuda_ext" ./
#
# 4. Install system dependencies (Ubuntu/Debian):
#    sudo apt-get update
#    sudo apt-get install -y libsndfile1 ffmpeg sox libsox-fmt-all
